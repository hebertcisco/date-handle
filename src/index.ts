import { DateHandle } from './core/date-handle';

const { future, futureFully, past, pastFully, date, yesterday } = DateHandle;
export { future, futureFully, past, pastFully, date, yesterday };
export default DateHandle;
